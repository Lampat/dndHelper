import 'package:flutter/material.dart';

class SpellsScreen extends StatelessWidget {
  static const routeName = '/spells-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('this is the spells screen'),),
    );
  }
}